# Alpaca paper trading keys (required for GetTrading on the local dev server).
# Generate at https://app.alpaca.markets/paper/dashboard/overview → "API Keys".
ALPACA_PAPER_KEY_ID=
ALPACA_PAPER_SECRET=

# Alpaca live trading keys (only set when you're ready to flip the toggle to live).
ALPACA_LIVE_KEY_ID=
ALPACA_LIVE_SECRET=
